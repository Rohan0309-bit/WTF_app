
'use client';

// This page is no longer needed as the workout session now happens in a dialog.
// Keeping the file to prevent build errors if it's referenced somewhere, but it can be removed.
export default function WorkoutSessionPage() {
    return null;
}
